CC0 1.0 Universal — Public Domain Dedication

This dedication covers the measurement data this study produced and nothing else:

  results/**/*.json       per-request records, incidents and environment snapshots
  analysis/**/*.txt       generated reports and tables
  analysis/**/*.csv
  analysis/**/*.png
  analysis/**/*.svg
  repro/**/*.json         reproducer findings and the phase lock files
  logs/**                 run logs, including the runs that were aborted
  models/SHA256SUMS*      checksums of the model files used

The list is by file type rather than by directory, and that is deliberate. It used to read
`analysis/**` and `repro/**`, which swept in `analysis/plot.py`, `analysis/plot_phase_m.py`,
`analysis/plot_qsmall_ladder.py` and `repro/llamacpp_27572.py` -- all of them source code, and all
of them excluded by the very next sentence. A downstream user could not tell which of the two
statements governed a `.py` file under `analysis/`.

It does NOT cover:

  * **Code**, wherever it sits. Every `.py` and `.sh` file in this repository is under LICENSE
    (MIT), including the ones under `analysis/` and `repro/`.
  * **`repro/*.bundle`**. Those are git bundles of upstream llama.cpp commits, carried so that a
    reproduction does not depend on a pull-request ref that has already moved. They are upstream
    source under upstream copyright and licence; see NOTICE. Nothing here waives anything in them.
  * Any other upstream or third-party material (see NOTICE).

To the extent possible under law, thc1006 has waived all copyright and related or neighboring
rights to the files listed above. The work is published from Taiwan.

The full text of CC0 1.0 is at https://creativecommons.org/publicdomain/zero/1.0/legalcode

No warranty of any kind is given. The dedication does not grant any rights in the model weights,
prompts sourced from elsewhere, or trademarks that may appear in the data.
