============================================================================================
DOES ONE BUILD PRODUCE THE SAME OUTPUT TWICE?
============================================================================================
  the same binary, the same card, the same prompts, greedy, run twice

  w=1  baseline    25/25 byte-identical between the two runs
  w=3  mtp-n2      25/25 byte-identical between the two runs
  w=4  mtp-n3      25/25 byte-identical between the two runs
  w=5  mtp-n4      25/25 byte-identical between the two runs
  w=6  mtp-n5      25/25 byte-identical between the two runs
  w=8  mtp-n7      25/25 byte-identical between the two runs

  overall 150/150

  The build is deterministic. Every difference measured between builds is therefore a
  build difference, and forced_down2 changing the width-1 baseline while its width-1
  kernels are byte-identical SASS is a real effect that still needs naming.
