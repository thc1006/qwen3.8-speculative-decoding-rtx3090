============================================================================================
FORCED-WARP INTERVENTION
============================================================================================
  control       : results/phase_warp_v2_control.json
  control2      : results/phase_warp_v2_control2.json
  forced_down2  : results/phase_warp_v2_forced_down2.json
  forced_up     : results/phase_warp_v2_forced_up.json

  --- tables verified against the source each build was compiled from ---
    verified: control, control2, forced_down2, forced_up

  control holds 150 records

  --- the guard: two builds, one configure, no source difference ---
    control against control2: 150/150 byte-identical
    The guard holds, so a difference between control and a forced build is the table.

============================================================================================
FORCED_UP : GENERIC table w1->4, w3->4, w4->4, w5->4, w6->4, w8->4
============================================================================================
    widths whose warp count changed : [5, 6, 8]
    widths left at the stock count  : [3, 4]

  --- validity gates (registered: a failure here voids the comparison) ---
    [PASS] greedy baseline byte-identical across builds
           width 1 is at 4 warps in both builds; identical 25, differs 0
    [PASS] intervention did not touch widths it should not have
           widths [3, 4]: identical 50, differs 0
    [FAIL] intervention had a measurable effect where it was applied
           widths [5, 6, 8]: identical 75, differs 0

  The build differs but produced no output change at the widths it edited.
  That is not the registered 'neither moves': it means the edited table row never
  reached the kernel. Resolve that before reading anything else here.

============================================================================================
FORCED_DOWN2 : GENERIC table w1->4, w3->2, w4->2, w5->2, w6->2, w8->2
============================================================================================
    widths whose warp count changed : [3, 4]
    widths left at the stock count  : [5, 6, 8]

  --- validity gates (registered: a failure here voids the comparison) ---
    [PASS] greedy baseline byte-identical across builds
           width 1 is at 4 warps in both builds; identical 25, differs 0
    [PASS] intervention did not touch widths it should not have
           widths [5, 6, 8]: identical 75, differs 0
    [FAIL] intervention had a measurable effect where it was applied
           widths [3, 4]: identical 50, differs 0

  The build differs but produced no output change at the widths it edited.
  That is not the registered 'neither moves': it means the edited table row never
  reached the kernel. Resolve that before reading anything else here.
