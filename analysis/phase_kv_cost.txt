====================================================================================================
VERIFICATION-STEP COST MODEL     speedup = mean_len / k
  mean_len = (predicted_n - 1) / (predicted_n - accepted - 1)   [derived; the API has
            no verification-step count, and this is low by <1 %. See the docstring.]
====================================================================================================
[integrity] derived mean_len vs the server's printed mean len: 125 requests, mean gap -0.0069, worst 0.0748
            within the log's own %5.2f printing precision, so the derivation tracks the server.

[integrity] API counters vs llama.cpp log lines: 125 requests compared, 0 mismatched  -- two independent sources agree to the token

--- k by arm (pooled over classes and passes) ---
arm              spec             w    n   k mean    k sd         k range
mtp-n2           draft-mtp        3   25   1.4583  0.0052   1.451-1.470
mtp-n3           draft-mtp        4   25   1.7496  0.0050   1.742-1.762
dflash2-n4       draft-dflash     5   25   1.8930  0.0090   1.880-1.914
mtp-n5           draft-mtp        6   25   2.3055  0.0087   2.291-2.329
dflash2-n7       draft-dflash     8   25   2.7278  0.0176   2.698-2.774

--- TEST 1: is the overhead charged to REJECTION? ---
    Model a rejection-proportional cost r (decode-steps per rejected draft token):
        k = k_verify + r * w * (1 - acceptance),  w = realised draft len / forward
        => dk/d(acceptance) = -r * w,   so  r = -slope / w
    A state-rollback account of the overhead predicts r > 0, i.e. slope < 0.
    The model holds w constant within an arm. Where the server reuses a surviving
    draft tail instead of re-drafting, w moves WITH acceptance and the slope picks
    that up rather than any rejection cost - biased positive, which reads as r < 0,
    which is this test's own conclusion. Arms whose w varies are fitted and shown,
    and excluded from the bound.

arm                 n    accept range      w  w cv%     r2                   r [95 %]  reading
mtp-n2              2  0.466-0.921   1.99   0.26  0.159       -0.01 [-0.01, -0.00]  clears zero by only 0.11 half-widths - inside the known undercoverage; would be -0.3% of k
mtp-n3              3  0.344-0.867   2.98   0.32  0.023       -0.00 [-0.01, +0.00]  no rejection cost detected (interval contains zero)
dflash2-n4          4  0.152-0.839   3.97   0.45  0.336       -0.01 [-0.01, -0.00]  clears zero by only 0.77 half-widths - inside the known undercoverage; would be -0.7% of k
mtp-n5              5  0.194-0.761   4.96   0.47  0.124       -0.00 [-0.01, +0.00]  no rejection cost detected (interval contains zero)
dflash2-n7          7  0.090-0.723   6.93   0.61  0.257       -0.01 [-0.01, -0.00]  clears zero by only 0.40 half-widths - inside the known undercoverage; would be -1.1% of k

  Largest upper 95 % limit on r, over the 5 arm(s) where the model applies: +0.00293
  decode-steps per rejected draft token (mtp-n3). The bound is an upper confidence
  limit, not the largest point estimate: the maximum of several noisy points is
  biased upward and is not a bound on anything.
  At that limit the rejection term accounts for at most +0.22 % of the cycle cost (mtp-n3),
  which is the comparable figure: r is per rejected draft token, so arms at different widths
  carry the same total cost at different r.
  Acceptance spans roughly 0.090-0.921 in this data - nearly a ten-fold range -
  so a rejection-driven overhead of any consequence would have shown up here.
  Reading: the verification overhead is paid per position VERIFIED, not per
  draft REJECTED. This does not say rollback is free; it bounds how much of
  the observed cost rollback can account for.
  Not established, and not treated as overturning the reading either way: mtp-n2 (0.11 half-widths below zero), dflash2-n4 (0.77 half-widths below zero), dflash2-n7 (0.40 half-widths below zero)

--- TEST 2: k vs verification width, per method ---
    MMVQ dispatch limit 8, from the analyser's constant: this file predates harness/kernel_facts.py
    fitting k = k0 + c*(w-1). `c` is the marginal cost of one more verified
    position in serial-decode-step equivalents, over the whole cycle rather than
    attributed to any one component; see the note under the fits.
  draft-dflash   MMVQ widths [5, 8]  ->  k0=0.7799  c=0.2783  r2=1.0000
                     k0 0.7761 to 0.7833   c 0.2772 to 0.2795   nominal 95 %, 25 prompts resampled within class
                     k(w=1) extrapolates to 0.7799, BELOW the floor of 1.0: the line implies a cycle cheaper than a plain decode step at zero draft depth, which no configuration can be.
                     so k(w) is concave here and the FIRST extra position costs more than c. Refit with k(1) pinned at the floor: k0=0.9732  c=0.2456  r2=0.9961  (11.8 % change in c)
                     the pin moves the fit materially, so the linear form does not reach zero depth and neither coefficient is a mechanism.
                     baseline    41.9 tok/s -> one decode step 23.875 ms; k0 = 18.619 ms, c = 6.644 ms per extra position  [6.618, 6.673]
      w= 5  k=1.8930  fit=1.8930  resid=+0.0000
      w= 8  k=2.7278  fit=2.7278  resid=+0.0000
  draft-mtp      MMVQ widths [3, 4, 6]  ->  k0=0.8986  c=0.2818  r2=0.9999
                     k0 0.8969 to 0.9004   c 0.2811 to 0.2824   nominal 95 %, 25 prompts resampled within class
                     k(w=1) extrapolates to 0.8986, BELOW the floor of 1.0: the line implies a cycle cheaper than a plain decode step at zero draft depth, which no configuration can be.
                     so k(w) is concave here and the FIRST extra position costs more than c. Refit with k(1) pinned at the floor: k0=0.9727  c=0.2623  r2=0.9969  (6.9 % change in c)
                     the pin moves the fit materially, so the linear form does not reach zero depth and neither coefficient is a mechanism.
                     lack of fit across widths: residual se on the slope 0.0033 (3 widths, 1 dof), against the prompt bootstrap's half-width 0.0006
                     the line is a 5.2x poorer description of these widths than prompt-to-prompt scatter alone would suggest, so `c` is a chord over the widths fitted and not a constant marginal cost.
                     baseline    41.9 tok/s -> one decode step 23.860 ms; k0 = 21.440 ms, c = 6.723 ms per extra position  [6.708, 6.738]
      w= 3  k=1.4583  fit=1.4621  resid=-0.0038
      w= 4  k=1.7496  fit=1.7439  resid=+0.0058
      w= 6  k=2.3055  fit=2.3074  resid=-0.0019

--- the same fit on requests that never diverged from their baseline ---
  draft-dflash   only 2 widths keep two or more non-diverging records; not fitted
  draft-mtp      c on all   75 = 0.2818   c on the  15 that never diverged = 0.2834   +0.6 %

  Two methods with independent drafters, fitted separately. What `k` measures is
  the whole speculative cycle: target verification, the drafter's own forwards,
  sampling, launch and synchronisation, output extraction and any per-step state
  management. The two methods share every one of those except the drafter.
  The two fits share 0 width(s) and cannot be compared on a common range.

  Separating the components needs per-context CUDA-event timing, a replay that
  skips drafter compute, or a profiler decomposition - none of which this file has.

--- implied optimum ---
    mean_len saturates with depth while k grows linearly, so speedup = mean_len/k has
    an interior maximum in principle. Whether the measured ladder shows one is read
    off the data below rather than asserted. An earlier version of this line stated
    that the ladder falls monotonically over every width measured. That was true of
    the dense phases it was written against and false the moment Phase M ran an MTP
    ladder that peaks at n-max 2 on both targets, and nothing would have caught it.
    These are best TESTED points. Whether a peak clears noise is analyze.py's paired
    intervals, not this table.
  draft-dflash   n4=1.508x  n7=1.218x
                 -> best tested n-max = 4   (falls monotonically)
                 -> smoothing k through the fit over MMVQ widths [5, 8] puts the peak at n-max 4 (preserves it; same mean_len, so not independent evidence)
  draft-mtp      n2=1.596x  n3=1.527x  n5=1.329x
                 -> best tested n-max = 2   (falls monotonically)
                 -> smoothing k through the fit over MMVQ widths [3, 4, 6] puts the peak at n-max 2 (preserves it; same mean_len, so not independent evidence)
