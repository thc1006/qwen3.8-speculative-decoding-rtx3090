================================================================================================
PHASE B -- IS THE COST PER DRAFTED TOKEN, OR PER REJECTED TOKEN?
================================================================================================
450 speculative requests over 6 arms, 25 prompts, 3 passes.
forward-pass counts are DERIVED, not exact, on 450 of 450 requests (the server does not expose draft_n_verif_steps); see cost_model.collect() for the bias this leaves and its size.

------------------------------------------------------------------------------------------------
1. IDENTIFICATION: can these arms tell the two regressors apart?
------------------------------------------------------------------------------------------------
arm                       n  drafted/fwd  rejected/fwd   accept    ms/fwd    excess
mtp-n3-p.00              75        2.985         1.320    0.558    42.644   +18.284
mtp-n3-p.50              75        2.067         0.601    0.683    40.046   +15.686
mtp-n3-p.75              75        1.343         0.174    0.848    36.527   +12.167
mtp-n7-p.00              75        6.930         4.689    0.323    72.318   +47.957
mtp-n7-p.50              75        3.567         1.581    0.526    52.456   +28.096
mtp-n7-p.75              75        1.911         0.422    0.768    41.873   +17.513

drafted-to-rejected ratio per arm, which is what tells the two one-parameter models apart:
    mtp-n7-p.00              1.48
    mtp-n7-p.50              2.26
    mtp-n3-p.00              2.26
    mtp-n3-p.50              3.44
    mtp-n7-p.75              4.53
    mtp-n3-p.75              7.71
  ratio spans 5.22x across arms.
  The gate sweep moves the mix by more than the depth sweep moves the totals, which is
  exactly what makes a like-for-like comparison of the two models possible.

corr(drafted/fwd, rejected/fwd) across arm means = +0.9963. High correlation is
expected here and does NOT block the comparison below: a one-parameter model is identified
by the ratio spread above, not by the regressors being uncorrelated. It does block the JOINT
fit at the end of this report, which is why that section is reported last and hedged.

------------------------------------------------------------------------------------------------
2. MATCHED PAIRS: one regressor moves, the other barely does
------------------------------------------------------------------------------------------------
mtp-n3-p.50 -> mtp-n7-p.75
    drafted/fwd  2.067 ->  1.911 (-0.155), rejected/fwd  0.601 ->  0.422 (-0.180)
    rejected moves, drafted held. ms/fwd change: +1.83 [+1.01, +2.71]  <-- clears zero by only 1.19 half-widths

------------------------------------------------------------------------------------------------
3. MODEL COMPARISON: two one-parameter models, fitted and scored separately
------------------------------------------------------------------------------------------------
H2' says the excess is proportional to drafted tokens; H2 says rejected tokens. Each is a
single free parameter, so each is identified whatever the two regressors' correlation is.

Fitted on the extensive form: excess_ms = coefficient * count, where excess_ms is
total decode time minus what the same number of plain forwards cost on that prompt.
model                         ms per token           RSS   R2 (origin)
H2'  cost per drafted               7.2204  133020089.15        0.9802
H2   cost per rejected             10.1986 1168750337.97        0.8256

  RSS(rejected model) - RSS(drafted model) = +1035730248.82 [+987243421.64, +1080656400.06]   clears zero by 21.14 half-widths
  Positive means the drafted-token model fits better.

  per-arm mean residual, so a reader can see WHERE each model fails:
    arm                        obs ms    H2 pred  H2-prime pred
    mtp-n3-p.00                2840.5     2247.0         3349.7
    mtp-n3-p.50                2606.6     1085.9         2418.6
    mtp-n3-p.75                2257.2      347.6         1704.0
    mtp-n7-p.00                6560.2     6999.9         6848.4
    mtp-n7-p.50                3803.2     2271.4         3400.8
    mtp-n7-p.75                2752.5      658.0         2025.8

  A one-token model cannot be the whole story: the drafter runs its own forward pass every
  step whether or not the gate lets it extend, so part of the excess is per STEP, not per
  token. Both models above under-predict the heavily gated arms, which is what that looks
  like. Adding the step term to each:

    model                                ms/step   ms/token             RSS       R2
    step + drafted   (H2-prime)            4.064      6.167     51645850.77   0.9923
    step + rejected  (H2)                 10.660      6.928    215702754.54   0.9678

    RSS(step+rejected) - RSS(step+drafted) = +164056903.77 [+132119341.39, +194805884.31]   clears zero by 4.22 half-widths

  WHY THIS SURVIVES A FORWARD-PASS COUNT THAT cost_model.py REFUSES TO USE.
  F comes from the same derivation whose integrity check fails on this phase, and
  cost_model.py now refuses to print k, c or k0 when it does. It does not follow that
  this comparison fails with it, and the difference is worth stating rather than
  asserting: `drafted` and `rejected` are exact counters, and F enters only through
  the offset F*tau0, so a systematic error in F moves the offset and not the
  regressors. Refitting with F shifted, which is the direction and size the
  derivation is known to be wrong by:
     F shift   one-parameter winner   ms/step  ms/token
          -2                drafted     4.315     6.195
          -1                drafted     4.189     6.181
          +0                drafted     4.064     6.167
          +1                drafted     3.940     6.153
  The winner does not change and the coefficients move by about a percent, so the
  comparison does not rest on F being exact. What it would still take from an exact
  count is the ABSOLUTE ms/step and ms/token, which are offsets away from it.

------------------------------------------------------------------------------------------------
4. JOINT FIT, reported last because it is the part that is NOT identified
------------------------------------------------------------------------------------------------
  beta_drafted : +7.64 [+7.55, +7.76]  ms per token per forward
  beta_rejected: -0.96 [-1.13, -0.83]  ms per token per forward

  One coefficient is NEGATIVE. Rejecting a draft cannot save time and neither can drafting
  one, so this is not a measurement: it is what least squares does when two regressors point
  in nearly the same direction and it splits one effect into a large positive and a small
  negative. The split is an artefact. The SUM is what the data constrains, and section 3
  is where the answer is.
