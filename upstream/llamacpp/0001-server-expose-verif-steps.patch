From 3c17c2e1eef2a652da38a7e7d20657ea245d8777 Mon Sep 17 00:00:00 2001
From: thc1006 <84045975+thc1006@users.noreply.github.com>
Date: Tue, 25 Aug 2026 03:57:46 +0800
Subject: [PATCH] server : expose the speculative verification-step count per
 request

server_slot_stats already counts verification steps and the server uses the
count for the per-slot mean-length log line and for the Prometheus metrics
added in #24536. The per-request JSON does not carry it, so a client has to
reconstruct it from draft_n, draft_n_accepted and predicted_n, and that
reconstruction is off by one because the first generated token comes from the
prompt pass rather than a decode forward.

Adds it to the speculative block of server_slot_stats::to_json(), and extends
the existing speculative server test, which fails against master with
KeyError: 'draft_n_verif_steps'.

Signed-off-by: thc1006 <84045975+thc1006@users.noreply.github.com>
---
 tools/server/server-common.cpp              | 5 +++--
 tools/server/tests/unit/test_speculative.py | 7 ++++++-
 2 files changed, 9 insertions(+), 3 deletions(-)

diff --git a/tools/server/server-common.cpp b/tools/server/server-common.cpp
index 4f5b820..79980db 100644
--- a/tools/server/server-common.cpp
+++ b/tools/server/server-common.cpp
@@ -80,8 +80,9 @@ json server_slot_stats::to_json() const {
     };
 
     if (n_draft_tokens > 0) {
-        base["draft_n"]          = n_draft_tokens;
-        base["draft_n_accepted"] = n_draft_accepted;
+        base["draft_n"]             = n_draft_tokens;
+        base["draft_n_accepted"]    = n_draft_accepted;
+        base["draft_n_verif_steps"] = n_draft_verif_steps;
     }
 
     return base;
diff --git a/tools/server/tests/unit/test_speculative.py b/tools/server/tests/unit/test_speculative.py
index 5837195..c29fd8b 100644
--- a/tools/server/tests/unit/test_speculative.py
+++ b/tools/server/tests/unit/test_speculative.py
@@ -47,7 +47,12 @@ def test_with_and_without_draft():
     server.start()
     res = server.make_request("POST", "/completion", data=request)
     assert res.status_code == 200
-    assert res.body["timings"]["draft_n"] > 0
+    timings = res.body["timings"]
+    assert timings["draft_n"] > 0
+    assert timings["draft_n_accepted"] <= timings["draft_n"]
+    # the verification-step count cannot be derived from the other two: the same acceptance
+    # fraction at two draft widths gives two different step counts
+    assert timings["draft_n_verif_steps"] > 0
     tokens_draft = res.body["tokens"]
 
     assert tokens_no_draft == tokens_draft
-- 
2.47.3

